import { Router } from 'express';

// User routes — mounted at /api/users
// Implemented in Phase 4. Planned endpoints:
//   GET    /search?q=
//   GET    /:userId
//   PATCH  /me
//   PATCH  /me/password
//   PATCH  /me/avatar

const router = Router();

export default router;
