import { Router } from 'express';

// Notification routes — mounted at /api/notifications
// Implemented in Phase 13. Planned endpoints:
//   GET    /
//   PATCH  /:notificationId/read
//   PATCH  /read-all
//   DELETE /:notificationId

const router = Router();

export default router;
