// Composer: text input, attachments, reply context, send button.
// Placeholder structure — full implementation in Phase 6.
export default function MessageInput({ children }) {
  return (
    <div className="border-t p-4">{children}</div>
  )
}
