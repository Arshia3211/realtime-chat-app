// Hover/long-press menu: reply, edit, delete, react.
// Placeholder structure — full implementation in Phase 10.
export default function MessageActions({ children }) {
  return (
    <div className="flex items-center gap-1">{children}</div>
  )
}
